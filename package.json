{
    "name": "@pookiesoft/bongbot-quote",
    "version": "0.1.0",
    "description": "BongBot QuoteDB command module",
    "main": "./dist/index.js",
    "type": "module",
    "exports": {
        ".": {
            "types": "./dist/index.d.ts",
            "import": "./dist/index.js"
        }
    },
    "files": [
        "dist",
        "!dist/standalone.*"
    ],
    "publishConfig": {
        "registry": "https://npm.pkg.github.com"
    },
    "scripts": {
        "build": "bun build --target bun --outdir dist --minify --sourcemap src/standalone.ts",
        "build:lib": "node esbuild.config.mjs --lib && npx tsc --emitDeclarationOnly",
        "prepublishOnly": "npm run build:lib",
        "build-dev": "docker build --secret id=NODE_AUTH_TOKEN,env=NODE_AUTH_TOKEN -t bongbot-quote . && docker run --rm --env-file .env --volume ./logs:/app/logs bongbot-quote",
        "test": "tsc -p tsconfig.test.json && cross-env NODE_OPTIONS=--experimental-vm-modules jest --json --coverage --outputFile=report.json --passWithNoTests --silent",
        "typecheck": "tsc -p tsconfig.test.json"
    },
    "peerDependencies": {
        "discord.js": "^14.26.3"
    },
    "devDependencies": {
        "@pookiesoft/bongbot-core": "^1.6.1",
        "@swc/core": "^1.16.2",
        "@swc/jest": "^0.2.39",
        "@types/jest": "^30.0.0",
        "better-sqlite3": "^13.0.1",
        "cross-env": "^10.1.0",
        "discord.js": "^14.26.3",
        "esbuild": "^0.28.0",
        "jest": "^30.2.0",
        "jest-junit": "^17.0.0",
        "msw": "^2.13.6",
        "prettier": "^3.8.3",
        "typescript": "^6.0.3"
    }
}
